snowio_connector:

    # Job titles
    jobs:
        incomplete_export:
            title: Snow.IO CSV incomplete export (categories, attributes, attribute options, and families)
            archive.title: Zip CSV files
            post.title: Post Zip file
        complete_export:
            title: Snow.IO CSV complete export (products, variant groups, categories, attributes, attribute options, and families)
            archive.title: Zip CSV files
            post.title: Post Zip file

    # Form labels and help guides
    form:
        exportDir:
            label: Export Directory
            help: The directory to store CSV and ZIP files.
        url:
            label: Url
            help: The URL where to make the request after file generation.
        applicationId:
            label: Application Id
        secretKey:
            label: Secret Key
